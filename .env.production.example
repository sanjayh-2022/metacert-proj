# Production Environment Variables for MetaCert
NODE_ENV=production

# Database Configuration (AWS RDS)
# Replace with your actual RDS endpoint, username, and password
DATABASE_URL="mysql://admin:YOUR_RDS_PASSWORD@your-rds-endpoint.region.rds.amazonaws.com:3306/metacert"

# NextAuth Configuration
# Replace with your actual domain
NEXTAUTH_URL=https://your-domain.com
NEXTAUTH_SECRET=your-super-secret-production-key-minimum-32-characters

# Google OAuth Configuration
# Use the same credentials from development or create production ones
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Blockchain Configuration
NEXT_PUBLIC_ALCHEMY_SEPOLIA_API=your-alchemy-api-key
NEXT_PUBLIC_SMART_CONTRACT_ADDRESS=your-contract-address
JWT_SECRET=your-jwt-secret-key
NEXT_PUBLIC_ACCOUNT_PRIVATE_KEY=your-account-private-key

# Optional: Local MySQL for Docker Compose (if not using RDS)
MYSQL_ROOT_PASSWORD=secure-root-password-for-local-mysql

# Server Configuration
PORT=3000
EXPRESS_PORT=8080

# Security
SECURE_COOKIES=true
TRUST_PROXY=true

# Logging
LOG_LEVEL=info